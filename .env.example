# Backend Environment Variables

# Environment
ENVIRONMENT=development

# Logging Configuration
LOG_LEVEL=debug

# Server Configuration
HOST=127.0.0.1
PORT=3001

# Database Configuration (for Fase 3)
DATABASE_URL=postgresql://user:password@localhost/portfolio

# Redis Configuration (optional, for rate limiting)
# REDIS_URL=redis://localhost:6379

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
FRONTEND_ORIGIN=http://localhost:3000

# ============================================================================
# Authentication & Security
# ============================================================================
# JWT secret for access tokens (use long random value in production)
JWT_SECRET=replace-with-secure-random-secret-min-32-chars
# Optional separate secret for refresh tokens (defaults to JWT_SECRET)
REFRESH_TOKEN_SECRET=replace-with-secure-random-secret-min-32-chars

# Admin credentials (use one of ADMIN_PASSWORD or ADMIN_HASH_PASSWORD)
ADMIN_EMAIL=admin@example.com
# Plain password (will be hashed automatically)
ADMIN_PASSWORD=admin123
# OR pre-hashed password (bcrypt hash) - preferred for production
# ADMIN_HASH_PASSWORD=$2b$12$...

# Token expiry (optional, defaults in code: access=15m, refresh=7d)
# JWT_EXPIRES_IN=15m
# REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================================================
# Payload Encryption (Fase 4 - Optional)
# ============================================================================
# Shared secret for encrypting request/response payloads (server-to-server)
# Generate with: openssl rand -base64 32
# PAYLOAD_ENCRYPTION_SECRET=your-32-byte-base64-secret
# Set to true to require encrypted payloads in production
# REQUIRE_PAYLOAD_ENCRYPTION=false
